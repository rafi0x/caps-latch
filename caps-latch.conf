# caps-latch configuration
#
# How long Caps Lock must be held down before it switches ON, in
# milliseconds. Switching it back OFF is always instant.
#
# Set to 0 to make activation instant too, restoring stock behaviour.
#
# After editing:  sudo systemctl restart caps-latch

delay_ms = 1000
